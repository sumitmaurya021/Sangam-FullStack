<%= stylesheet_link_tag 'auth_premium', media: 'all', 'data-turbo-track': 'reload' %>
<script src="/assets/auth_premium.js" data-turbo-track="reload"></script>

<div class="ap-viewport">
  <!-- Dynamic Ambient Backdrop & Particle Canvas -->
  <div class="ap-ambient-bg">
    <canvas id="apCanvas" class="ap-canvas"></canvas>
    <div class="ap-glow-orb ap-glow-orb--1"></div>
    <div class="ap-glow-orb ap-glow-orb--2"></div>
    <div class="ap-glow-orb ap-glow-orb--3"></div>
  </div>
  <div class="ap-grid-overlay"></div>

  <!-- Split-Screen Container -->
  <div class="ap-split-wrapper">
    <!-- LEFT SIDE: Animated App Showcase -->
    <%= render "users/shared/showcase_panel" %>

    <!-- RIGHT SIDE: Authentication Form Panel -->
    <div class="ap-form-panel">
      <div class="ap-card">
        <div class="ap-card-border-glow"></div>

        <!-- Header -->
        <div class="ap-header">
          <h1 class="ap-title">Set up 2FA Protection</h1>
          <p class="ap-subtitle">Protect your account with Google Authenticator, 1Password, or Authy</p>
        </div>

        <!-- Setup Step 1: QR Code -->
        <div style="display: flex; flex-direction: column; align-items: center; text-align: center; margin-bottom: 1rem;">
          <p style="font-size: 0.78rem; color: #94a3b8; margin-bottom: 0.5rem;">1. Scan QR code in your authenticator app</p>
          <div style="background: #ffffff; padding: 0.5rem; border-radius: 12px; box-shadow: 0 8px 20px rgba(0,0,0,0.3); display: inline-block;">
            <%= raw @qr_svg %>
          </div>
          <% if @provisional_secret.present? %>
            <div style="margin-top: 0.5rem; font-size: 0.7rem; color: #64748b;">
              Key: <code style="background: rgba(255,255,255,0.08); padding: 0.15rem 0.35rem; border-radius: 4px; color: #a5b4fc;"><%= @provisional_secret %></code>
            </div>
          <% end %>
        </div>

        <!-- Setup Step 2: Form with 6-box OTP -->
        <%= form_with url: enable_two_factor_auth_path, method: :post, html: { class: 'ap-form', autocomplete: 'off' } do |f| %>
          <%= f.hidden_field :otp_code, id: 'hidden_setup_otp' %>

          <div class="ap-form-group">
            <label class="ap-label" style="justify-content: center;">2. Enter 6-digit code</label>
            <div class="ap-otp-container" data-otp-container data-otp-target="#hidden_setup_otp">
              <input type="text" class="ap-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]*" autofocus aria-label="Digit 1" />
              <input type="text" class="ap-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]*" aria-label="Digit 2" />
              <input type="text" class="ap-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]*" aria-label="Digit 3" />
              <input type="text" class="ap-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]*" aria-label="Digit 4" />
              <input type="text" class="ap-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]*" aria-label="Digit 5" />
              <input type="text" class="ap-otp-box" maxlength="1" inputmode="numeric" pattern="[0-9]*" aria-label="Digit 6" />
            </div>
          </div>

          <!-- Submit Button -->
          <%= f.button type: :submit, class: "ap-btn-primary" do %>
            <span class="ap-btn-text">Enable 2FA Protection</span>
            <div class="ap-spinner"></div>
          <% end %>
        <% end %>

        <!-- Footer Switcher -->
        <div class="ap-footer">
          <%= link_to "← Back to Profile / Settings", edit_user_registration_path, class: "ap-footer-link" %>
        </div>

        <div class="ap-copyright">
          © <%= Time.current.year %> Sangam. All rights reserved.
        </div>
      </div>
    </div>
  </div>
</div>
