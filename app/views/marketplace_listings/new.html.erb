<div class="marketplace-page">
  <%= render 'shared/header' %>
  
  <div class="market-form-wrapper">
    <div class="market-form-card">
      <h2 style="font-size: 32px; font-weight: 800; color: var(--market-text-main); margin-bottom: 32px;">Create New Listing</h2>
      <%= render 'form', listing: @listing %>
    </div>
  </div>
</div>

<%= stylesheet_link_tag 'shared/marketplace', 'data-turbo-track': 'reload' %>
